const fonts = ['Poppins', 'Roboto', 'Helvetica', 'Arial', 'sans-serif'];
const typography = {
  h1: {
    fontFamily: fonts.join(','),
  },
  h2: {
    fontFamily: fonts.join(','),
  },
};

export default typography;
